# Keep the two templates as the only entry points. An issue opened with no
# template is an issue that has to be interviewed afterwards, and that
# conversation usually never happens.
blank_issues_enabled: false
