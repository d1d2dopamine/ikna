# ai-audits — пять read-only аудитов (2026-09-26, GLM 5.3-flash)

Подготовительная работа под следующие партии. Ничего в коде не изменено;
каждый пункт — с файлом:строкой и рекомендацией. Пункты, попавшие в планы,
нужно сверить с `docs/modern_PLAN-0.11.md` перед реализацией. Требует ревью.

## 1. Track G — карта usability-партии

**Отключённые действия (G-1/G-2):**

| Место | Что отключается | Условие | Объяснение пользователю |
| --- | --- | --- | --- |
| `shared/ui/catalog/CatalogRows.kt:177,186` | кнопки строк каталога | `enabled = !blocked` | проверить при реализации: показывается ли причина блокировки рядом (аналог `browseUnavailableText`) |
| `shared/ui/decks/DeckList.kt:76` | блок «Сегодня» | `total > 0` | объяснение есть: `deck.008` («нет карточек на сегодня») — эталон |
| `shared/ui/session/CardStack.kt:441` | тап по карточке | `tapEnabled` | проверить условие и добавить причину, если состояние неочевидно |
| `desktop/.../AddDeckPane.kt:228,400,409` | импорт/создание колоды | `!busy`, `filled.isNotEmpty()` | полузаявленное: пустое поле видно само, но `busy`-состояние без подписи |
| `shared/ui/settings/SettingsChrome.kt:153,169` | степперы ± | `value > min / < max` | самоочевидно (границы шкалы) — не требует объяснения |

**Деструктивные действия (G-5):** двухшаговые подтверждения уже есть и у
стирания (`set.071`–`set.076`: «Стереть всё» → «ТОЧНО СТЕРЕТЬ ВСЁ» → «Нажми ещё
раз…»), и у удаления колоды (`dp.007`–`dp.008`). Изоляция DEV-профиля описана
(`dev.003`/`dev.004`). Пробел: в текстах стирания не сформулирован **охват**
(что именно стирается: колоды + история + настройки? отдельный DEV-профиль
не трогается?). Рекомендация: одна фраза охвата в каждом подтверждении.

**Иерархия «Редкого» (G-3):** внутри секции сейчас без подгрупп: блок
разработчика (включение/выключение, сценарии, reseed), диагностика
(`diag.003`–`diag.008`), pseudo-locale, инспектор; на Android сюда же примыкают
backup/restore. Предложение: три подгруппы — «Разработчик», «Диагностика»,
«Опасное» (wipe/reset) — моно-заголовками, как в основных секциях.

**Терминология (G-4):** чисто. FSRS встречается только в осознанных местах
(оптимизатор `optimizer.001/021`, миграция `mig.002`); «Governor» в
пользовательских строках не светится. Пункт можно закрывать после беглой
проверки остальных языков.

**Поверхности без общей рамки (G-8):** `DeckList.kt:83` (блок «Сегодня» — Inner,
осознанно), `DeckList.kt:151` (строка колоды — Outer ✓), `BrowseCard.kt:104`
(ссылка-источник — Outer ✓), `CardStack.kt:441,528` (тап карточки и источник —
без рамки), `StatsContent.kt:114` (раскрытие блока статистики — без рамки,
есть `onClickLabel`), `TransientNotice.kt:45`. Решать по одному: карточка
сессии намеренно «молчит» (DESIGN.md) — тап без рамки корректен; раскрывающиеся
блоки статистики — кандидаты на рамку.

## 2. Инвентаризация прямых углов (Track J, скруглённый вариант)

Ключевой факт: **ни один composable не читает `MaterialTheme.shapes`** —
`IknaShapes` (все `RoundedCornerShape(0.dp)`) объявлен в `Theme.kt`, но не
потребляется. Ненулевых `RoundedCornerShape/CornerRadius` в коде нет вообще —
кроме параметрических радиусов Signal Frame (`cornerRadius = 6–8.dp`), то есть
скругления сегодня есть только у самой рамки. `clipToBounds` — одно место
(`DeckList.kt:150`).

Что придётся решить варианту 2 (по группам):

| Группа | Места | Решение |
| --- | --- | --- |
| Форма-система | `Theme.kt` `IknaShapes` | завести `IknaShapeVariant` (angular/rounded) и радиусы по классам (чипы/поля/карточки/панели) |
| Границы контейнеров | `Flat.kt` чипы (560), кнопки (352), поля (602, 640), тумблер (490); `DeckList.kt` строка; `PaletteTiles.kt` плитки; `BrowseCard.kt` блоки | радиус границы из варианта; сейчас все `.border(1.dp …)` квадратные |
| Canvas-примитивы | `MemoryLattice.kt` (зерно — **оставить квадратным**: пиксельный язык), `IknaProgress` (ячейки), `SignalFrame.kt` (рамка уже скруглена параметром), `WindowTitleBar.kt` иконки окна (штрихи) | решётка и прогресс — квадрат при обоих вариантах; кнопки окна — радиус по варианту |
| Карточка сессии | `CardStack.kt`, `BrowseCard.kt` блоки | радиус карточки — самое видимое отличие вариантов |
| Печати/метки | `DeckMark` 52dp, `EN`-печати | печатный квадрат — маркер идентичности; решать отдельно (возможно, квадрат при обоих вариантах) |

Риск forks-кода отсутствует: точек вариации мало, все через `IknaShapes`/
один параметр варианта. Пункт Track J дополнен этой записью.

## 3. a11y-обход

- **`IknaIconButton`:** все реальные call-site'ы передают `label` ✓ (единственное
  «срабатывание» grep'а — определение самой функции в `Flat.kt:246-253`).
- **`clickable` без `role`:** `DeckList.kt:83,151`, `BrowseCard.kt:104`,
  `CardStack.kt:441,528`, `TransientNotice.kt:45` — добавить `role` и/или
  `onClickLabel`; `PaletteTiles.kt` (Role.RadioButton ✓) и `Flat.kt` (Role.Button ✓)
  — образцы. `StatsContent.kt:114` уже с `onClickLabel` ✓.
- **a11y-ключи:** определены `a11y.001–012, 014, 015` — **в нумерации нет
  `a11y.013`** (когда-то удалён?). Проверку «ключ определён, но не
  используется» не удалось завершить из-за регэксп-ошибки скрипта — доделать
  при G-работе (все ключи выглядят потреблёнными: 001 back, 006 toggle,
  007 stats-open, 010/012/015 deck icons).
- **Декоративные Canvas** (`IknaMemoryField`, `IknaMemoryAmbientStrip`,
  `IknaDeckHeaderPaint`, `IknaProgress`, wordmark): `clearAndSetSemantics`
  нигде не стоит; Canvas без семантики скринридеры обычно игнорируют, но
  явный `invisibleToUser` на декоративных поверхностях — дешёвая страховка
  (особенно для новых полос с анимацией).
- **Строки с вложенными кнопками** (`IknaDeckRow`: две иконки + тумблер внутри
  кликабельной строки): проверить mergeDescendants, чтобы TalkBack не читал
  строку как один комок — кандидат в G-11.

## 4. Вердикты по подозрениям аудита

- **`ChunkShape.kt` (дубли TERMINAL) — ложная тревога.** В файле ровно один
  `TERMINAL` (`private val TERMINAL = charArrayOf('.', '!', '?', '…', '。', '！', '？')`,
  строка 58) и одно использование (строка 126, `isSentence`). Дублей нет, файл
  чистый и документированный.
- **Дубли Anki-файлов app/desktop — реальное дублирование.** Построчно
  идентичные по объёму копии: `AnkiText.kt` (348 = 348), `AnkiProto.kt`
  (88 = 88), `DeckLanguage.kt` (225 = 225) в `app/src/main/java/dev/ikna/data/anki/`
  и `desktop/src/main/kotlin/dev/ikna/desktop/anki/`. Это чистая логика без
  платформенных зависимостей — кандидат на переезд в `shared/jvmShared`
  (совпадает с открытым пунктом Track E «duplicate platform implementations»).
  Импортеры при этом разные по замыслу: у app — `AnkiImporter` (644) поверх
  Room-инжеста, у desktop — `AnkiPackage` (654) + `AnkiShape`/`AnkiSql`
  (разбор .apkg); их унификация — отдельная задача после Catalogue v2, не в 0.11.

## 5. Отложенный долг (UNSCHEDULED / REFACTOR / Track K)

| Идея | Откуда | Статус | Рекомендация |
| --- | --- | --- | --- |
| Локальные снапшоты перед разрушающими операциями | UNSCHEDULED | не начато; условие «если есть доказанный failure mode» | держать; связать с G-5 (охват подтверждений) |
| Локальная диагностика установки | UNSCHEDULED | **частично сделано**: на десктопе в «Редком» есть diagnostics (`diag.003–008`, `diagnosticsText(container)`) | сверить список UNSCHEDULED с выдачей diagnostics и закрыть пункт |
| Аудит настроек (устаревшие контролы) | UNSCHEDULED | пересекается с G-3 (иерархия «Редкого») | слить с G-3 в одну работу |
| Shared-target UX аудит | UNSCHEDULED | = G-12 («deck membership never implies a second memory») | оставить в Track G, из UNSCHEDULED сослаться |
| Пояснение первого запуска | UNSCHEDULED | онбординг уже есть (4 слайда); пункт про «маленький первый батч» | проверить текст онбординга: слово «адаптивно» там есть? при G-работе |
| Состояния загрузки каталога | UNSCHEDULED | не начато | оставить до реальных жалоб |
| Матрица совместимости виджета | UNSCHEDULED | регулярная maintenance | оставить |
| Split `LearningRepository` (~1290 строк) | REFACTOR + Track K | `REVIEW AFTER 0.11`; правила сплита записаны (один mutex, append-only reviews, move-first двумя коммитами) | после 0.11, отдельная задача |
| Split `SettingsScreen` (~1710 строк) | REFACTOR + Track K | `REVIEW AFTER 0.11` | после 0.11; естественно следует за G-3 |

Вывод: UNSCHEDULED не содержит блокеров 0.11; два пункта уже частично
реализованы или переехали в Track G. REFACTOR — единственный источник
структурных работ, оба сплита честно отложены до конца 0.11.
